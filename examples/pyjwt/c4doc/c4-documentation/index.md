# C4 Architecture Documentation

> Generated with `ai-craftkit` skill: `c4doc`  
> Source: `https://github.com/jpadilla/pyjwt.git` at commit `7144e4534c34810f4525dc4578a32addd8212cff`  
> Prompt: `Create the c4 documentation for the pyjwt repo here in this workspace.`

## Purpose

This folder documents the architecture of PyJWT using a practical C4-style structure sized to the repository that actually exists.

PyJWT is a Python library, not a deployable service, so this documentation stops at the views that clarify library boundaries and key module responsibilities.

## Start Here

| Document | Purpose |
|---|---|
| [Generation Report](generation-report.md) | Explains what was generated, skipped, inferred, and needs review |
| [Architecture Model](architecture-model.md) | Shared source-of-truth tables for elements, relationships, and omissions |
| [System Context](system-context.md) | C1 view showing who uses PyJWT and which external systems matter |
| [JWT Library Components](components/jwt-library.md) | C3 view of the main responsibility boundaries inside the `jwt` package |

## Documentation Scope

| Field | Value |
|---|---|
| Repository | `pyjwt` |
| Main system | `PyJWT` |
| Primary language(s) | `Python` |
| Primary runtime(s) | `CPython`, `PyPy` |
| Primary deployment target(s) | Python applications that install the package from source or a built distribution |
| Last generated/updated | `2026-07-07` |
| Generated by | `c4doc` |

## Recommended Reading Order

1. [Generation Report](generation-report.md)
2. [System Context](system-context.md)
3. [JWT Library Components](components/jwt-library.md)
4. [Architecture Model](architecture-model.md)

## Diagram Legend

The diagrams use ordinary Mermaid flowcharts, not Mermaid C4 syntax.

| Label Prefix | Meaning |
|---|---|
| `Person:` | Human actor or role |
| `Software System:` | Software system boundary |
| `External System:` | System outside the documented boundary |
| `Component:` | Responsibility boundary inside the library package |

## Confidence Legend

| Confidence | Meaning |
|---|---|
| Confirmed | Directly supported by repository evidence |
| Inferred | Reasonably inferred from source/configuration/naming |
| Unknown | Not known from repository contents |
| Needs review | Should be validated by maintainers |
| Omitted | Intentionally excluded from the model |

## Current Documentation Set

| View | Status | Reason |
|---|---|---|
| [System Context](system-context.md) | Generated | The library has a clear caller boundary and one architecture-relevant external integration point |
| Container | Skipped | The repository packages a library rather than separate runnable or deployable units |
| [Component View](components/jwt-library.md) | Generated | The `jwt` package has stable responsibility boundaries worth documenting |
| Code View | Skipped | API and implementation details are already better served by the existing source and Sphinx API docs |
| Deployment | Skipped | No deployment topology, infrastructure manifests, or hosted runtime are present in the repository |
| Dynamic Flow | Skipped | Most interactions are direct library calls; no stable cross-process workflow needed a permanent sequence view |

## Maintenance Notes

Update this documentation when:

- the public API surface changes in a way that reshapes the internal responsibility boundaries
- new runtime integrations are added beyond local token processing and optional JWKS fetches
- a deployable service, CLI, or separate package is introduced
- optional cryptography support changes the algorithm or key-handling architecture significantly

## Open Review Items

- [ ] Confirm that `PyJWT` is the preferred system name for architecture docs.
- [ ] Confirm whether the host Python application should stay modeled as an external system in the context view.
- [ ] Confirm whether maintainers want a second focused component view for the JWKS client subsystem.